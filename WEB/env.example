# ============================================
# WEB服务环境变量配置模板
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 根据实际情况修改各个配置项
# 3. 注意：.env 文件包含敏感信息，不要提交到版本控制系统
# ============================================

# ============================================
# 服务配置
# ============================================
# WEB服务端口（仅 HTTPS + HTTP/2，默认 8888 → 容器 443）
WEB_PORT=8888

# ============================================
# 代理配置（可选）
# ============================================
# 后端API网关地址（用于nginx代理配置）
# GATEWAY_URL=http://gateway:8080

# ============================================
# SSL配置
# ============================================
# 证书由 install_linux.sh 自动生成到 conf/ssl/server.crt|key
# 也可手动替换为正式证书（保持文件名不变）
# SSL_CERT_FILE=ssl/server.crt
# SSL_KEY_FILE=ssl/server.key

# ============================================
# 其他配置
# ============================================
# 时区设置
TZ=Asia/Shanghai

