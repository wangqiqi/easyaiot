
# 表结构同步产物（明文整库备份与差异 SQL，绝不可入库）
# 媒体节点离线 Docker 镜像包（体积大，勿提交）
# 排除 postgresql 初始化脚本
# AI 模型权重（大文件由 ModelScope 或安装脚本下载，不随仓库分发）
AI/models/
**/alert_images/
# alert_images 文件夹
# APP 构建产物、依赖与 pnpm 缓存
APP/dist/
APP/docs/.vitepress/cache
APP/docs/.vitepress/dist
APP/.eslintcache
APP/.hbuilderx
APP/logs/
APP/node_modules/
APP/.playwright-cli/
APP/.pnpm-store/
APP/private.key
APP/src/types/*
!APP/src/types/auto-import.d.ts
!APP/src/types/async-component.d.ts
!APP/src/types/async-import.d.ts
APP/.stylelintcache
APP/tmp/
**/backup/
# backup 文件夹
# BlueJ files
.build-cache/**
!.build-cache/README.md
*.class
.claude/changed-files-hook.sh
# Claude Code 个人配置与本地钩子（仅本机，不随仓库分发）
.claude/settings.local.json
# CMake 构建目录
.codegraph/
# CodeGraph 本机索引数据（仅保留忽略规则文件）
# COMPILE 打包产物与临时目录（不随仓库分发；与 COMPILE/.gitignore 对齐）
# Compiled class file
COMPILE/dist/
COMPILE/.panel-version
COMPILE/**/*.spec.bak
COMPILE/.venv-build/
COMPILE/work/
*.ctxt
.deps/
DEVICE/.idea
DEVICE/iot-gb28181/iot-gb28181-biz/snap
/DEVICE/snap/
/DEVICE/target
# Docker 构建依赖宿主机缓存（项目根 .build-cache，由 install_linux*.sh 创建）
*.ear
**/.env.arch
.flattened-pom.xml
hs_err_pid*
# IDE / 包管理器缓存 / 测试缓存（仅本机，不随仓库分发）
.idea
*.iml
# install 脚本按架构自动生成的 Docker 构建配置（DOCKER_PLATFORM / BASE_IMAGE）
/iot-api/*/target
/iot-auth/target
/iot-common/*/target
/iot-gateway/target
/iot-modules/*/target
/iot-registry/target
/iot-visual/*/target
*.jar
# legacy TASK paths (compat)
# Local SDK downloads
*.log
# Log file
# Mobile Tools for Java (J2ME)
.mtj.tmp/
# 本地烟雾测试 / 临时 ini 与日志（不随仓库分发）
.tmp/
*.nar
# Node Agent 离线 pip wheel（体积大，勿提交）
NODE/get-pip.py
NODE/pip-wheels/

# Node Agent 运行时环境变量（含 token，勿提交）
NODE/agent.env

# Sentinel Agent 离线 pip wheel（体积大，勿提交）
SENTINEL/pip-wheels/

# Sentinel Agent 运行时环境变量（含 token，勿提交）
SENTINEL/agent.env
# Package Files #
.pnpm-store/
*.pyc
**/.pytest_cache/
*.rar
# RTC 本地运行数据与 vendor 源码（go2rtc 由 install_linux.sh vendor 拉取，不随仓库分发）
RTC/data/
RTC/.env
RTC/vendor/go2rtc/
# RUNTIME 构建产物、离线包、本地验证与模型权重（由 install/export/download 脚本生成，不随仓库分发）
RUNTIME/atomic.env
RUNTIME/.atomic-test/
RUNTIME/.atomic-verify/
RUNTIME/.bundle-runtime/
RUNTIME/build/
RUNTIME/build-linux/
RUNTIME/cmake-build-debug/
RUNTIME/cmake-build-local/
RUNTIME/cmake-build-orangepi/
RUNTIME/deploy.env
RUNTIME/VERSION
RUNTIME/models/*
!RUNTIME/models/coco.names
RUNTIME/config/task_*.ini
RUNTIME/.test-hwaccel/
RUNTIME/.test-yolo-link/
RUNTIME/.test-*/
# .scripts/docker 中间件数据目录和日志目录
.scripts/docker/db_data/
.scripts/docker/.deploy_profile
.scripts/docker/dify_data/
.scripts/docker/fuxa_data/
.scripts/docker/gpustack_data/
.scripts/docker/logs/
.scripts/docker/milvus_config/
.scripts/docker/milvus_data/
.scripts/docker/minio_data/
.scripts/docker/mq_data/
.scripts/docker/nodered_data/
.scripts/docker/redis_data/
.scripts/docker/.runtime_images_pulled
.scripts/docker/srs_data/
.scripts/docker/standalone-logs/
.scripts/docker/taos_data/
.scripts/docker/vscode_data/
.scripts/docker/.web_deploy_profile_built
.scripts/media-cluster/images/*.tar
# .scripts/nexus3 数据目录
.scripts/nexus3/nexus_data/
!.scripts/postgresql/00-init-databases.sh
.scripts/postgresql/schema-sync/backups/
.scripts/postgresql/schema-sync/schema_diffs/
.scripts/zlmediakit/conf/config.ini
.scripts/zlmediakit/log/
.scripts/zlmediakit/www/
# SITE 构建产物、依赖与 pnpm 缓存（与 SITE/.gitignore 一致）
SITE/dist/
SITE/dist-prebuilt/
SITE/logs/
SITE/node_modules/
SITE/.pnpm-store/
target/
*.tar.gz
# TASK/build/
TASK/build-linux/
# TASK/cmake-build-debug/
# TRANSFORM 本地运行与构建产物（不随仓库分发）
!TRANSFORM/capabilities/transform-capability-backup/src/main/java/com/basiclab/iot/transform/capability/backup/
!TRANSFORM/capabilities/transform-capability-backup/src/test/java/com/basiclab/iot/transform/capability/backup/
TRANSFORM/data/
TRANSFORM/dist/
TRANSFORM/.env.docker
TRANSFORM/.idea/
venv
**/.venv
# VIDEO 运行日志与告警截图（任务运行时生成）
VIDEO/logs/
# VIDEO 运行时生成的 RUNTIME/推流转发 ini（含设备凭证，勿提交）
VIDEO/data/runtime_config/
# VIDEO 人脸模型（大文件由 install 脚本或 WEB 人脸库页下载，不随仓库分发）
VIDEO/data/models
VIDEO/face_capture_head_yolov8.onnx
VIDEO/face_matching_insightface_buffalo_l/
VIDEO/face_rec.onnx
VIDEO/ffmpeg-master-*.tar.xz
# VIDEO 离线 ffmpeg 运行时打包产物（体积大，不随仓库分发）
VIDEO/.bundle-ffmpeg/
# VIDEO 离线 pip wheels 缓存（体积大，不随仓库分发）
VIDEO/.bundle-wheels/
# VIDEO 内置 YOLO：.pt→onnx 导出缓存与旁路 .names（保留已跟踪的 yolo*.pt；勿提交导出产物）
VIDEO/yolo*.onnx
VIDEO/yolov*.onnx
VIDEO/yolo*.names
VIDEO/yolov*.names
VIDEO/yolo*.names.json
VIDEO/yolov*.names.json
# AI 根目录同名导出缓存（权重 .pt 若已跟踪则保持；导出 onnx 不入库）
AI/yolo*.onnx
AI/yolov*.onnx
AI/yolo*.names
AI/yolov*.names
AI/yolo*.names.json
AI/yolov*.names.json
# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
.vscode/
# IDEA 工作区模板的 VS Code 配置需要入库（覆盖上面的 .vscode/）
!IDEA/workspace-template/.vscode/
!IDEA/workspace-template/.vscode/**
*.war
# WEB 日志目录（与 WEB/.gitignore 中 logs/*、!logs/.gitkeep 一致）
# WEB 预构建前端缓存（runtime_image.sh 提取，体积大，勿提交）
/WEB/dist-prebuilt-full/
/WEB/dist-prebuilt-mini/
/WEB/dist-prebuilt-standard/
# WEB docker build 输出日志（与 WEB/.gitignore 中 docker-build-logs/*、!.gitkeep 一致）
/WEB/docker-build-logs/*
!/WEB/docker-build-logs/.gitkeep
/WEB/logs/*
!/WEB/logs/.gitkeep
*.zip
# apt 离线下载包（install 脚本走 apt-get install；勿在仓库根目录 apt download 后误提交）
*.deb
# apt 离线包缓存目录（apt download / 离线装机用，统一放这里）
.offline-debs/

# Maven 构建临时备份（本地调试/权限问题时的产物，勿提交）
**/target.bak_old/
**/target.bak_root_owned/

# 安装脚本生成的本地 docker env（模板见 .env.docker）
.scripts/docker/.env

# IDEA 运行时数据、本地配置与虚拟环境（与 IDEA/.gitignore 对齐；工作区 clone / git-reference 体积大，勿提交）
IDEA/.data/
IDEA/.venv/
IDEA/idea.env
IDEA/logs/
IDEA/workspaces/
IDEA/__pycache__/

# HARNESS 运行时数据与本地配置（与 HARNESS/.gitignore 对齐；容器挂载工作区时会在仓库根生成 AGENTS.md 软链）
HARNESS/.data/
HARNESS/harness.env
/AGENTS.md
/HARNESS-README.md

# ZLMediaKit 挂载目录（与 docker-compose 中 ../zlmediakit 对应）；运行时数据
